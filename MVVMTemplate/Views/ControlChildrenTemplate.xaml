<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MVVMTemplate.Views.ControlChildrenTemplate">
    <Grid>

        <Label Text="{Binding Title}" Grid.Row="0" Grid.Column="0" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" FontSize="28" BackgroundColor="DimGray" TextColor="White"/>
        <Button Text="{Binding RedButtonText}" Command="{Binding AddControlChildrenCommand}" Grid.Row="1" Grid.Column="0" BackgroundColor="IndianRed"/>
        <StackLayout x:Name="ExampleStackLayout" Grid.Row="2" Grid.RowSpan="4"  Grid.Column="0" BackgroundColor="WhiteSmoke"/> 

    </Grid>
</ContentPage>
